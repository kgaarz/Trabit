<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="de.trabit.reportApp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Trabit"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name="de.trabit.reportApp.ThirdAddActivity">


        </activity>

        <activity android:name="de.trabit.reportApp.SecondAddActivity" />
        <activity android:name="de.trabit.reportApp.FirstAddActivity" />
        <activity android:name="de.trabit.reportApp.SearchActivity" />
        <activity android:name="de.trabit.reportApp.OverviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <meta-data
                android:name="preloaded_fonts"
                android:resource="@array/preloaded_fonts" />
    </application>

</manifest>